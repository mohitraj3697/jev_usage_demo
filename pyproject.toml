[project]
name = "jev"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Mohit Raj", email = "mohitraj3697@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "typesafe-sdk>=0.7.1",
]

[project.scripts]
jev = "jev:main"

[build-system]
requires = ["uv_build>=0.12.19,<0.13.0"]
build-backend = "uv_build"
